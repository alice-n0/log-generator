name: order-service

image:
  repository: order-service
  tag: latest

env:
  PAY_SERVICE:
    BASE_URL: http://pay-service:8080
    TIMEOUT: 2000

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  cpu: 50
